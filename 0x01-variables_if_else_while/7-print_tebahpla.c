#include <stdio.h>
/**
 * main - entry
 * Description: print the alphabet in reverse
 * Return: Always 0
 */

int main(void)
{
	char le;

	for (num = z; num >= a; le--)
		putchar(le);
	putchar("\n");

	return (0);
}
